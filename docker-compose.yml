version: '3.6'
services:
    api:
        build:
            context: ./backend/api
            dockerfile: Dockerfile
        ports:
            - 5000:5000
        extra_hosts:
            - "host.docker.internal:host-gateway"
    web-app:
        build:
            context: ./frontend/web-app
            dockerfile: Dockerfile
        ports:
            - 3000:3000
